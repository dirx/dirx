## 1. Restore the logo treatment

- [x] 1.1 Add the existing discrete random hue selection and yellow-to-green brightness and saturation adjustments to the shared header logo, and verify the applied filter uses only the established palette and adjustment rules.

## 2. Verify the site

- [x] 2.1 Build the static site with `pnpm build` and verify all primary pages load with the header logo colorized after each page load or reload.
